---
services:
  bookshelf:
    image: ghcr.io/pennydreadful/bookshelf:hardcover
    container_name: bookshelf
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - USER=${USER}
      - PASS=${PASS}
    volumes:
      - ~/.config/bookshelf:/config
      - /mnt/lake1t/downloads:/downloads
      - /mnt/seagate4t/books:/books
    ports:
      - 8787:8787
    restart: unless-stopped
