services:
  jellyfin:
    image: 'jellyfin/jellyfin'
    restart: 'unless-stopped'
    network_mode: 'host'
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    ports:
      - 8096:8096
    volumes:
      - './cache:/cache'
      - './config:/config'
      - './logs:/logs'
      - /mnt/seagate4t:/seagate4t
      - /mnt/lake1t:/lake1t
    extra_hosts:
      - 'host.docker.internal:host-gateway'