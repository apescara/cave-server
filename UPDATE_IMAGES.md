# Update docker images in the lxc

Cus I'm 2 lazy to remember the commands and I don't trust an auto updater

100:

```bash
cd /mnt/lake1t/cave-server/docker/100/
docker compose pull && docker compose up -d && docker system prune -a --volumes 
```

101:

```bash
cd /mnt/lake1t/cave-server/docker/101/
docker compose pull && docker compose up -d && docker system prune -a --volumes 
```

102:

```bash
cd /mnt/lake1t/cave-server/docker/102/
docker compose pull && docker compose up -d && docker system prune -a --volumes 
```

103:

```bash
cd /mnt/lake1t/cave-server/docker/103/
docker compose pull && docker compose up -d && docker system prune -a --volumes 
```

104:

```bash
cd /mnt/lake1t/cave-server/docker/104/
docker compose pull && docker compose up -d && docker system prune -a --volumes 
```

105:

```bash
cd /mnt/lake1t/cave-server/docker/105/
docker compose pull && docker compose up -d && docker system prune -a --volumes 
```
